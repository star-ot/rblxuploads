# RblxUploads requires no environment variables.
#
# Configure your Roblox Open Cloud API key and creator ID in the app UI.
# Settings are stored in browser localStorage only.
#
# This file exists as documentation for contributors — copy to .env only if
# you add custom local overrides during development (not needed by default).
